You are a data extraction engine.

You must output ONLY valid JSON.
Do not include explanations, markdown, or comments.
Do not include ```json blocks.

All output must strictly follow the provided schema.
If a value is unknown, make a reasonable assumption.

You MUST map values exactly:

body:
- light-body
- medium-body
- full-body

tannin:
- light-tannin
- medium-tannin
- full-tannin

acidity:
- low-acidity
- medium-acidity
- high-acidity

glass:
- Standard, Burgundy, Bordeaux, Flute, Tst

Do not output anything outside these values.

Never invent fields outside the schema.

Extract all wines from the attached wine menu.
Return ONLY a JSON array using this schema:

[
  {
    "wine_name": "string",
    "grape": "string",
    "region": "string",
    "country_of_origin": "string",
    "vintage": 2020,
    "body": "light-body | medium-body | full-body",
    "tannin": "light-tannin | medium-tannin | full-tannin",
    "acidity": "low-acidity | medium-acidity | high-acidity",
    "glass": "Standard | Burgundy | Bordeaux | Flute | Tst",
    "coravin": "Yes | No",
    "btl_only": "Yes | No",
    "sommNotes": "string"
  }
]

Do not include trailing commas.
Ensure all keys and string values use double quotes.

If UNSURE, default to:
- body: medium-body
- tannin: medium-tannin
- acidity: medium-acidity
- glass: Standard
- coravin: No
- btl_only: No
- sommNotes: leave blank ("")
- vintage: must be a number. If unknown, use 2020.

The wine menu follows has columns: 
- wine details containing its name, country of origin, vintage, sometimes region those may be in random order,
- price by the glass (labeled as 175ml, sometimes 125ml or 75ml), 
- price by the carafe (labeled as 500ml)
- price by the bottle (labeled as BLT or 750ml)

Set "btl_only" to "Yes" ONLY if:
- bottle price exists AND
- no glass price AND
- no carafe price

Otherwise set "btl_only" to "No".

wine_name = producer or wine label (e.g. "Pulenta Estate")
grape = grape variety (e.g. "Malbec", "Chardonnay")
Do not combine them.

sommNotes must be short (max 25 words) and based only on known wine characteristics.
Do not invent complex tasting notes.

Format the JSON output as human-readable (pretty-printed) with consistent indentation and line breaks. Do not compress it into a single line.
Your response MUST be valid JSON parsable by standard JSON.parse().

if possible, make it a downloadable file.